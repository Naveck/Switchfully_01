package advanced;

import java.util.regex.Pattern;

/**
 * Change the <pattern> to make the methods work correctly
 */
public class RegexCodelab01 {

    public static boolean isTheWordApple(String word) {
        return Pattern.matches("<pattern>", word);
    }

    public static boolean isAllLowerCaseLetters(String word) {
        return Pattern.matches("<pattern>", word);
    }

    public static boolean isAllUpperCaseLetters(String word) {
        return Pattern.matches("<pattern>", word);
    }

    public static boolean isAllLetters(String word) {
        return Pattern.matches("<pattern>", word);
    }

    public static boolean isAllLettersOrSpaces(String word) {
        return Pattern.matches("<pattern>", word);
    }
}
